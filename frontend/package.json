{
  "name": "show-me-frontend",
  "version": "1.0.0",
  "description": "Show Me Frontend",
  "scripts": {
    "build": "esbuild app.js --bundle --minify --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
